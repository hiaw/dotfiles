# -*- mode: snippet -*-
# name: React reducer
# key: reducer
# --
const initialState = {

};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    case $1:
      return {...state, $2};

    default:
      return state;
  }
}